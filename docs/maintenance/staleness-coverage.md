---
title: Doc staleness — signal coverage measurement
area: maintenance
---

# Doc staleness — signal coverage measurement

> Generated by `scripts/measure-doc-staleness-coverage.mjs`. Do not edit by hand.
> This is a MEASUREMENT, not a gate — it exits 0 no matter what it finds.

## Why this exists

Every doc gate in this repo is structural: it checks that links resolve, that
edges are valid, that a retired substrate is not named. None of them reads what
a page *says*. Before building one that does (BI-3E5969DF), the question that
decides whether it is worth building is not how precise it would be — it is how
much of the corpus any signal can reach at all. A detector cannot report on a
page it has no edge to, at any precision.

## The finding

**197 of 682 published doc pages (28.9%) carry a doc-impact edge.**
The other **485** (71.1%) carry none, and are invisible to
the doc-impact gate by construction — not because the gate is imprecise, but
because there is no edge along which it could ever be told to look.

| Signal | Pages |
| ------ | ----- |
| Code edges only | 150 |
| Route edges only | 43 |
| Both | 4 |
| **No edge at all** | **485** |

### The uncovered remainder is not all a gap

Of those 485, **468 reference no repo file anywhere in their text** — no link, no
backticked path. They are WSID profession doctrine and founder-kernel principle
pages: they describe how to decide, not what the code does. **No code-edge
derivation will ever reach them**, and counting them as a shortfall argues
forever for widening that has already run out.

The genuinely recoverable set is **17** page(s) — those that cite a repo file but
whose citation the current derivations do not turn into an edge.

The timestamp detector (`build-docs-staleness.mjs`, BI-AA5DFEA2) currently reports **166** stale
candidate(s). That number is small because its reach is small, not because the
corpus is fresh — it can only ever flag a page that links a repo file.

## Coverage by area

Darkest first. These are the areas where a semantic gate would be blind today.

| Area | Pages | Covered | Coverage |
| ---- | ----- | ------- | -------- |
| `marketing` | 4 | 0 | 0% |
| `ux-fit` | 2 | 0 | 0% |
| `professions` | 226 | 5 | 2.2% |
| `founder-kernel` | 137 | 7 | 5.1% |
| `triage` | 7 | 1 | 14.3% |
| `maintenance` | 4 | 1 | 25% |
| `(root)` | 3 | 1 | 33.3% |
| `dogfood` | 3 | 1 | 33.3% |
| `testing` | 41 | 14 | 34.1% |
| `runbooks` | 6 | 3 | 50% |
| `design` | 4 | 2 | 50% |
| `operations` | 21 | 12 | 57.1% |
| `architecture` | 101 | 60 | 59.4% |
| `security` | 9 | 6 | 66.7% |
| `user-guide` | 83 | 59 | 71.1% |
| `edge-node` | 7 | 5 | 71.4% |
| `personas` | 4 | 3 | 75% |
| `install` | 13 | 10 | 76.9% |
| `specs` | 3 | 3 | 100% |
| `strategy` | 2 | 2 | 100% |
| `dev` | 1 | 1 | 100% |
| `prompts` | 1 | 1 | 100% |

## What this implies for BI-3E5969DF

1. **Widening is close to exhausted.** Of the pages any code-edge derivation could
   reach, 197 of 214 (92.1%) now carry an edge. The headline 28.9% understates
   the gate because most of what it excludes is doctrine, not undocumented code.
   Further widening buys single-digit page counts; the cheap direction is spent.
2. **Reach is no longer the binding constraint — precision is.** That inverts
   this report's earlier recommendation, and it is the change that makes a
   semantic detector worth designing rather than deferring.
3. **Gate on the addressable corpus, never on the whole.** A gate whose
   denominator includes principle pages will always look broken, and one that
   silently ignores them reports a coverage it does not have — the failure the
   [`gate-coverage-matches-blast-radius`](../founder-kernel/wiki/principles/gate-coverage-matches-blast-radius.md)
   principle names. Say which population is in scope, and measure against it.
4. **Still measure over-report before binding.** Nothing here says the semantic
   signal is accurate — only that it would now have something to look at.

