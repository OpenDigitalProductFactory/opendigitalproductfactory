# Auto-generated by fresh-install.ps1 (developer mode)
# Stores Docker volumes on H: drive and exposes ports to the host
# so you can run Next.js locally (pnpm dev) and connect from your IDE.
services:
  postgres:
    ports:
      - "5432:5432"
    volumes:
      - "H:/docker-data/dpf/pgdata:/var/lib/postgresql/data"
  neo4j:
    ports:
      - "7687:7687"
      - "7474:7474"
    volumes:
      - "H:/docker-data/dpf/neo4jdata:/data"
  qdrant:
    ports:
      - "6333:6333"
    volumes:
      - "H:/docker-data/dpf/qdrant_data:/qdrant/storage"
