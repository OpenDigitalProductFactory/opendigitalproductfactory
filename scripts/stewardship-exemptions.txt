# Stewardship retention exemptions (BI-C34E09B0).
#
# The third disposition. A persistent Prisma model normally declares itself
# either purgeable (PURGE_POLICIES) or regulated-retained (RETAINED_DATASETS) in
# apps/web/lib/operate/retention/policies.ts. Some models legitimately belong to
# NEITHER — and before this file, that was expressed only by ABSENCE, which is
# indistinguishable from the silent omission the gate exists to catch.
#
# Format:  ModelName  reason-slug  # rationale
#
# Valid reason slugs (closed set — packages: scripts/lib/stewardship-scope.mjs):
#   reference-data            Reference / lookup data. Superseded by
#                             replacement, never aged out.
#   config-singleton          Single-row (or per-org) configuration. Lifetime is
#                             the install's lifetime.
#   domain-lifecycle-managed  Rows are created and removed by their own domain
#                             lifecycle, so a time-based sweep races that owner.
#   derived-projection        Derived from a source of record; rebuilt or
#                             cascaded, never independently aged.
#
# DO NOT use this file to park a model you have not thought about — that is what
# the ratchet baseline (stewardship-scope-baseline.txt) is for. An entry here is
# a positive assertion that no retention window applies.
#
# Equally: do NOT resolve a gap by inventing a purge window. The retention engine
# EXECUTES PURGE_POLICIES; a fabricated window on reference data deletes it.

# ── Reference / lookup data ──────────────────────────────────────────────────
# Every model below is already declared "public" in
# packages/db/src/table-classification.ts, whose own tier definition reads
# "Reference / lookup data safe for any environment". These entries record that
# existing decision on the retention axis; they do not introduce a new judgement.

TaxonomyNode         reference-data  # Canonical product/skill taxonomy tree.
EaElementType        reference-data  # ArchiMate element type catalogue.
EaRelationshipType   reference-data  # ArchiMate relationship type catalogue.
EaRelationshipRule   reference-data  # ArchiMate legal-relationship matrix.
EaDqRule             reference-data  # EA data-quality rule definitions.
EaNotation           reference-data  # EA notation/rendering reference.
EaStructureRule      reference-data  # EA structural validity rules.
ViewpointDefinition  reference-data  # ArchiMate viewpoint catalogue.
StorefrontArchetype  reference-data  # Storefront archetype catalogue.
PlatformCapability   reference-data  # Platform capability catalogue.
Country              reference-data  # ISO country reference data.
Region               reference-data  # Sub-national region reference data.
City                 reference-data  # City reference data.
EmploymentType       reference-data  # HR employment-type lookup.
WorkLocation         reference-data  # HR work-location lookup.

# ── Domain-lifecycle-managed ──────────────────────────────────────────────────
# A row's meaningful lifecycle (when it stops being live evidence a human
# should still look at) is owned by an explicit domain action, not by age.

DecisionResolutionProposal  domain-lifecycle-managed  # BI-3D0FB84B. A drafted
  # resolution moves proposed -> accepted/amended/rejected/superseded/expired by a
  # human ruling or by its decision being settled elsewhere; it is never aged out on
  # a clock. Rows cascade with the DecisionInteraction they belong to, and a ruled
  # row is kept deliberately — the draft-versus-accepted delta is the only record of
  # whether the drafting was worth its cost, and a rejection is doctrine too.
WeightAdjustmentProposal  domain-lifecycle-managed  # BI-D88DFEEA. status transitions from
  # proposed to ruled/rejected only via an explicit human ruling
  # (ruleWeightAdjustmentProposal); a ruled/rejected row is retained
  # permanently as the audit record of that ruling. A generic age-based sweep
  # would race the still-open review queue and would delete governance
  # evidence a real regulated-retention floor doesn't apply to but the
  # decision ledger it's attached to (DecisionInteraction) does need intact.
AbsorptionPosture  reference-data  # Authored absorption classification (BI-ECO-001); superseded by replacement, never aged out. Reseeded idempotently from the vertical-incumbents manifest.
EdgeNodeCertificate  domain-lifecycle-managed  # Certificate records follow the Edge Node enrollment, rotation, revocation, and cascade-delete lifecycle; an age-based purge could remove an active authorization binding.
ProductLine  domain-lifecycle-managed  # Organization-owned hierarchy changes through explicit business-product lifecycle actions and organization cascade, never an age-based sweep.
Product      domain-lifecycle-managed  # Organization-owned product definitions change through explicit business-product lifecycle actions and product-line cascade, never an age-based sweep.
ProductObjective  domain-lifecycle-managed  # Product objectives close or archive through explicit outcome-review actions and follow the owning Product lifecycle; age alone cannot decide whether an objective is still governing work.
ProductObjectiveWork  domain-lifecycle-managed  # Objective-to-work links follow their ProductObjective and BacklogItem parents; an independent age sweep would sever active outcome traceability.
ProductOutcomeObservation  domain-lifecycle-managed  # Append-only observations and corrections follow the owning objective and its explicit review lifecycle; age-based deletion would break the outcome-learning chain.
DemandEvidenceLink  domain-lifecycle-managed  # Reviewed demand evidence is superseded or removed through the canonical BacklogItem lifecycle; an independent sweep would make investment scores unexplainable.
LifecycleEvent  domain-lifecycle-managed  # Append-only transition evidence follows the explicitly governed lifecycle of its referenced thing; age-based deletion would break architecture and accountability history.
PlateauMembership  derived-projection  # Baseline intervals are rebuilt from canonical operational evidence, while curated target membership is changed through the owning architecture lifecycle; neither is independently age-purged.
LifecycleGap  domain-lifecycle-managed  # Gap rows close, dismiss, or scope through explicit lifecycle-gap reconciliation and remain as decision provenance; age alone cannot decide whether the signal or linked work is still material.
OperationalSceneLayout  domain-lifecycle-managed  # Operator-authored geometry follows the owning organization's explicit layout lifecycle and cascades with the organization; an age-based sweep could delete an active floor plan.
Resource  domain-lifecycle-managed  # W19 unified resource: retires through explicit operating actions and follows its organization/storefront lifecycle; age cannot determine service status.
ResourceAvailability  domain-lifecycle-managed  # Follows its Resource and cascades with it; independent age-based deletion would silently reopen or close an active resource.
ResourceCapacityPool  domain-lifecycle-managed  # Pooled capacity follows its storefront's operating configuration; not an age-purgeable record.
ResourceCapacityAllocation  domain-lifecycle-managed  # Allocation ledger rows follow their booking/hold lifecycle (audit of seated demand); the W19 migration plan owns any future windowing decision.
HospitalityResource  domain-lifecycle-managed  # Physical venue resources retire through explicit operating actions and follow their organization/storefront lifecycle; age cannot determine whether a table or station is still in service.
HospitalityCapacityPool  domain-lifecycle-managed  # Aggregate kitchen, bake, and delivery capacity retires through explicit operating actions; an age sweep could remove a still-active capacity definition.
HospitalityResourceAvailability  domain-lifecycle-managed  # Availability windows follow their HospitalityResource and cascade with it; independent age-based deletion would silently reopen or close an active resource.
HospitalityCapacityAllocation  domain-lifecycle-managed  # Append-preserving capacity consumption follows booking/hold and explicit release lifecycles; an age sweep could erase live conflict and utilization evidence.
BeautyResource  domain-lifecycle-managed  # Fixed chairs, stations, and rooms retire through explicit beauty operations and follow their organization/storefront lifecycle; age cannot determine whether a resource remains active.
BeautyResourceService  domain-lifecycle-managed  # Service eligibility follows its resource and storefront service parents; independent aging would silently make a valid chair or room incompatible.
BeautyResourceAvailability  domain-lifecycle-managed  # Physical availability windows follow their BeautyResource and cascade with it; independent aging would silently reopen or close an active resource.
BeautyCapacityAllocation  domain-lifecycle-managed  # Append-preserving beauty capacity follows booking/hold and explicit release lifecycles; age-based deletion could erase live conflict and utilization evidence.
HospitalityServiceTurn  domain-lifecycle-managed  # A turn closes through explicit service-stage transitions and follows its organization/booking lifecycle; an age sweep could remove a live or still-auditable dining-room turn.
HospitalityServiceTurnEvent  domain-lifecycle-managed  # Append-only transition evidence follows its HospitalityServiceTurn cascade; independent aging would make seating, movement, and closure decisions unexplainable.
BusinessMetricRollup  derived-projection  # Recomputed from canonical operational sources and cascaded with its organization; it is replaceable projection state, not an independently aged record.
ProductOffering  domain-lifecycle-managed  # Organization-owned commercial packaging follows explicit offering status/effective-date actions and product cascade, never an age-based sweep.
CatalogItem  domain-lifecycle-managed  # Canonical ways-to-buy follow explicit catalog status/effective-date actions and offering cascade, never an age-based sweep.
ProductConfiguration  domain-lifecycle-managed  # Reusable configurations follow explicit publish/retire actions and catalog-item cascade, never an age-based sweep.
CatalogSku  domain-lifecycle-managed  # Reusable selling identities follow explicit publish/retire actions and catalog-item cascade, never an age-based sweep.
CatalogBundleComponent  domain-lifecycle-managed  # Package membership changes through explicit Catalog Builder effective-dated actions and catalog-item cascade, never an age-based sweep.
CatalogPriceList  domain-lifecycle-managed  # Business price lists have explicit status/effective dates and organization lifecycle; a generic age sweep could delete a still-referenced commercial definition.
CatalogPriceListEntry  domain-lifecycle-managed  # Item/SKU prices follow their price-list and catalog lifecycles with explicit effective dates, not record age.
CatalogPromotion  domain-lifecycle-managed  # Promotions retire through explicit status/effective dates and organization lifecycle; age alone is not a safe deletion signal.
CatalogPromotionItem  domain-lifecycle-managed  # Promotion targeting follows its promotion/catalog parents and is cascade-managed rather than independently aged.
CatalogChannelEligibility  domain-lifecycle-managed  # Channel eligibility is effective-dated commercial policy changed by explicit Catalog Builder actions, not a time-based retention sweep.
ProductSold  domain-lifecycle-managed  # Immutable commercial sale facts follow explicit cancellation/refund and organization lifecycles; age alone cannot determine their legal or reporting usefulness.
ProductSoldEvidence  domain-lifecycle-managed  # Typed transaction evidence follows its ProductSold and source-authority lifecycle; an independent age sweep would break commercial traceability.
ProductSoldParty  domain-lifecycle-managed  # Evidence-backed customer roles follow ProductSold and canonical party lifecycles rather than record age.
ProductSoldEntitlement  domain-lifecycle-managed  # Rights end through their explicit validity/status lifecycle and ProductSold cascade, not a generic age threshold.
ProductFulfillmentInstance  domain-lifecycle-managed  # Fulfillment records close through booking, rental, or supported-instance lifecycle transitions and ProductSold cascade.
ProductSoldComponentAllocation  domain-lifecycle-managed  # Non-additive package attribution follows its immutable ProductSold fact and is never independently aged out.
StorefrontOrderLineItem  domain-lifecycle-managed  # Normalized commercial lines follow the owning StorefrontOrder lifecycle; independent purging would corrupt order and sale evidence.
StockItem  domain-lifecycle-managed  # Operator-maintained supply master: retired through isActive and organization cascade, never aged out — a stocked item is current until the operator stops stocking it.
StorefrontItemComponent  domain-lifecycle-managed  # Recipe lines follow their sellable item and stock item via cascade; an age sweep would silently delete a live product's composition.
IncumbentCoverageAssessment  domain-lifecycle-managed  # Per-customer coverage verdicts (BI-548060D5); re-assessment supersedes the prior row, never time-aged.
JobRequisition  domain-lifecycle-managed  # Requisitions close/fill through their own status lifecycle (open→closed/filled), not a time-based sweep (BI-F3AEBF68).
WorkroomParticipant  domain-lifecycle-managed  # Roster rows cascade with their Workroom and are archived by membership lifecycle; a time sweep would drop a live room's Process Overseer.
WorkroomRelation  domain-lifecycle-managed  # Relation rows cascade with their Workrooms; a time sweep would drop a live contains/depends-on edge.
AsyncInferenceOperationTransition  domain-lifecycle-managed  # Ordered lifecycle evidence cascades with its owning AsyncInferenceOp; independent age purging would break cursor reconciliation and event audit truth.
RequisitionOpening  domain-lifecycle-managed  # Headcount slots follow their parent requisition via cascade; never independently aged.
JobPosting  domain-lifecycle-managed  # Postings are unpublished/closed by requisition lifecycle and cascade; not age-swept.
PipelineStage  domain-lifecycle-managed  # Stage definitions follow their requisition via cascade; not independently aged.
RecruitingSource  reference-data  # Source lookup superseded by operator edits, never aged out.
DispositionReason  reference-data  # Disposition-reason lookup table; superseded, never time-aged.

# ── Trust envelope over the decision kernel (EP-VERIFICATION-INTEGRITY) ───────
EvidenceReVerification  domain-lifecycle-managed  # Re-checks of a decision's cited evidence (BI-70FF9114); created and removed with their parent DecisionInteraction via cascade, never independently aged.
JurisdictionCriteriaProfile  reference-data  # Operator-authored jurisdiction criteria packs (BI-B84CD2D3); versioned and superseded by a newer version, never aged out.
MacVendorOui  reference-data  # IEEE OUI registry seeded from packages/db/data/oui.tsv (BI-9632B15B); replaced wholesale on reseed, never time-aged. A purge window would delete the lookup discovery depends on.
McpToolSession  domain-lifecycle-managed  # short-TTL discovery state; rows are removed by their own expiresAt sweep (sweepExpiredToolSessions + lazy delete on read), not the retention engine
FederationIntroductionCandidate  derived-projection  # Reconciled from a trusted introducer, expired/withdrawn by that exchange, and cascade-removed with its introducer link; it is never an independent source of trust.
ExternalChannelProjection  derived-projection  # Rebuilt or explicitly retired from canonical DPF source plus external-channel evidence; age alone cannot decide whether a live source-to-remote binding is still authoritative.
AnimalProfile  domain-lifecycle-managed  # Canonical animal identity follows explicit rescue lifecycle and organization retention; public listing changes never delete it.
AnimalCustodyEpisode  domain-lifecycle-managed  # Admission-to-outcome evidence closes through an explicit outcome and can be held; age cannot decide disposition.
AnimalCustodyEvent  domain-lifecycle-managed  # Append-only custody evidence follows its episode; independent aging would break legal-hold and transfer history.
AnimalAdoptionApplication  domain-lifecycle-managed  # Applications close through explicit screening decisions and remain placement provenance; age alone is not disposition.
AnimalPlacement  domain-lifecycle-managed  # Placement and return history changes only through explicit lifecycle actions and remains custody provenance.
CareRecord  domain-lifecycle-managed  # Care facts are corrected by supersession, may be held, and follow the animal's governed record lifecycle.
FinancialFund  domain-lifecycle-managed  # Effective-dated fund definitions follow finance governance; purging one would make posted journal dimensions uninterpretable.
PayComponentLine  domain-lifecycle-managed  # Payslip money lines (BI-EAC670F1); cascade-deleted with their parent Payslip and retained for as long as payroll/tax law requires that payslip. A purge window here would gut historical pay records.
EmployeeDeductionElection  domain-lifecycle-managed  # Standing deduction instructions (BI-EAC670F1); effective-dated and retired via RecordLifecycle so a closed run still explains what it deducted and why.
Vehicle  domain-lifecycle-managed  # Fleet/driver vehicles (BI-6D98AD8A); leave service via the RecordLifecycle convention so historical trips keep resolving their vehicle. Age alone must not delete a vehicle a prior reimbursement cites.
Trip  domain-lifecycle-managed  # Captured drives (BI-6D98AD8A); a trip that priced a reimbursement is accounting evidence and must survive as a row. Geometry minimisation is bounded by DriverLocationConsent.retentionDays and is enforced by a dedicated field-level job, NOT by row purge — a purge window here would delete claimed expense evidence.
TripClassificationRule  domain-lifecycle-managed  # Auto-classification rules (BI-6D98AD8A); retired via RecordLifecycle and retained so a historical trip can still explain which rule classified it.
DriverLocationConsent  domain-lifecycle-managed  # Consent grants and revocations (BI-6D98AD8A); the consent history IS the audit evidence for lawful capture. Revocation stops capture and must never delete the record that consent once existed.
MileageRatePlan  reference-data  # Org reimbursement rate plans (BI-E17E0034); superseded by a newer plan, never time-aged.
MileageRate  reference-data  # Effective-dated reimbursement rates (BI-E17E0034); a trip prices at the rate in force on its own date, so purging an expired rate would make historical reimbursements unreproducible.
TaxObligationPeriodComponent  domain-lifecycle-managed  # Component totals of a tax obligation period (BI-947F8703); owned by and cascade-deleted with their period, and a component of a FILED return is accounting evidence that must survive as a row. A purge window here would delete the figures a filed return was built from.
TaxDepositSchedule  domain-lifecycle-managed  # Effective-dated deposit cadence determinations (BI-947F8703); superseded by a later determination, never time-aged. Purging an expired schedule would destroy the evidence for why a past deposit was timed the way it was, which is exactly what a late-deposit penalty dispute turns on.
OAuthClient  domain-lifecycle-managed  # Registered MCP OAuth clients (BI-E4DFDCB0); retired by explicit revocation, which also revokes every token it holds. Age is the wrong axis: a client_credentials client used nightly by CI and a browser client used twice a year are equally live, and purging either by age would break a working connection with no operator action. Revocation is the lifecycle, and the row must survive it so the admin list and the consent audit trail can still name what was revoked.
PayrollTaxRule  reference-data  # Published statutory figures with citations (BI-8E1FD1BD); superseded by a later effective-dated rule, never time-aged. Purging an expired figure would make a closed payroll period unreproducible, and a rejected proposal is kept deliberately so the same wrong figure is not proposed again.
